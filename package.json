{
  "name": "consumer-service",
  "version": "1.0.0",
  "description": "Queue consumer service for the sporos alerts feature with adaptive concurrency and circuit breaker",
  "main": "dist/consumer.js",
  "scripts": {
    "build": "tsc",
    "start": "node dist/consumer.js",
    "dev": "ts-node consumer.ts",
    "clean": "rm -rf dist",
    "prebuild": "npm run clean"
  },
  "dependencies": {
    "pino": "^8.0.0",
    "@supabase/supabase-js": "^2.0.0",
    "dotenv": "^16.0.0"
  },
  "devDependencies": {
    "typescript": "^5.0.0",
    "ts-node": "^10.0.0",
    "@types/node": "^20.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}